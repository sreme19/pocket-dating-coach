gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
upload-keystore.jks
# Exception: the DEBUG keystore is intentionally committed so every build (CI
# included) shares one signature — debug keys use fixed public credentials
# (android/androiddebugkey) and are not secret. Release keys stay ignored above.
!app/debug.keystore
