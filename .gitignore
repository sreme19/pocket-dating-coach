node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Voice agent local env (contains secrets, never commit)
voice-agent/.env

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Book data (private)
/data/*.pdf

# Claude worktrees (local development)
.claude/worktrees/

# Android release signing (keystore files must never be committed)
*.jks
*.keystore
android/keystore.properties
android/app/release.keystore

# App Store Connect API keys — private keys, must never be committed
*.p8
mobile/.asc-keys/

# Firebase config (contains project-specific keys, should not be committed)
google-services.json
android/app/google-services.json

# Kiro MCP settings (contains API tokens)
.kiro/settings/mcp.json

# Supabase CLI local cache
supabase/.temp/

# App Store Connect signing keys (never commit)
mobile/.asc-keys/
*.p8

# iOS distribution signing certs — private keys, never commit
*.p12
*.pfx
mobile/.signing/
